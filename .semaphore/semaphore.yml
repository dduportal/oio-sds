blocks:
  - name: Set things up
    task:
      jobs:
        - name: Compile
          commands:
            - checkout
            - echo "Linting code"
            - echo "Compiling code"
  - name: Unit tests
    task:
      jobs:
        - name: Unit
          commands:
            - checkout
            - echo "Running unit tests"
version: v1.0
name: ''
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

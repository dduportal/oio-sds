version: v1.0
name: First pipeline
agent:
  machine:
    type: e1-standard-4 # 4 CPU 8 GB
    os_image: ubuntu1804
blocks:
  - name: Build
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: build with docker
          commands:
            - docker build --tag=sds-build-py2 --target=build-py2 ./
